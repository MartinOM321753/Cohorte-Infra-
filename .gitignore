# Secretos reales — nunca se versionan.
# Las plantillas (*.example) si se versionan: no contienen valores reales.
.env
.env.*
!.env.example
!.env.*.example

# Repos de aplicacion clonados aqui por el pipeline en tiempo de build
# (no son codigo de este repo; viven en Cohorte-IMSS y Cohorte-front)
/cohorte_test/
/client/
